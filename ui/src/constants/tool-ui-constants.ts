export const MAX_TOOL_CHARS = 80;

// Char cap for a backend-log object's stringified preview before it's
// truncated with "... (N more)" and only fully shown in the JSON viewer modal.
export const BACKEND_LOG_OBJECT_TRUNCATE_CHARS = 300;

// Shared cap for the approval bubble's args/diff preview columns, so both
// sides scroll internally at the same height instead of nesting scrollboxes.
export const MAX_APPROVAL_PREVIEW_HEIGHT_PX = 400;
