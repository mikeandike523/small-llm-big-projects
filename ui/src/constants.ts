/** Pixels from the bottom of a scrollable container within which the user is considered "at bottom". */
export const SCROLL_BOTTOM_THRESHOLD = 50

/** Throttle rate (ms) for scroll-to-bottom calls, preventing floods during rapid token streaming. */
export const SCROLL_THROTTLE_MS = 80
