# MySQL port is now assigned randomly by Docker (127.0.0.1::3306 in docker-compose.yml).
# Use `docker compose port mysql 3306` to discover the actual host port.
# No static port variables are needed here.
