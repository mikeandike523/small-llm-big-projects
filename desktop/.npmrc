# electron-forge/electron-packager require a flat node_modules to walk during
# packaging; pnpm's default isolated/symlinked layout breaks that.
node-linker=hoisted

# Supress warning for AI agents that use npm even though the project is clearly pnpm
config-extra-keys=node-linker